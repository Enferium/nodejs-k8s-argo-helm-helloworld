apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: "{{ template.name }}"  # Inherit name from ApplicationSet
spec:
  source:
    helm:
      chart: hello-world-k8s  # Replace with your chart name
      parameters:
        - name: values
          valueFile: "{{ .Values.file }}"
